<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <!-- =====================================================
         TAMAÑOS DE FUENTE — modificar solo aquí
         ===================================================== -->
    <sys:Double x:Key="FontSizeSmall">12</sys:Double>   <!-- labels de filtro, hints    -->
    <sys:Double x:Key="FontSizeBase">14</sys:Double>    <!-- texto general               -->
    <sys:Double x:Key="FontSizeMedium">15</sys:Double>  <!-- encabezados de columna      -->
    <sys:Double x:Key="FontSizeLarge">16</sys:Double>   <!-- títulos de sección          -->

    <!-- =====================================================
         ESTILOS BASE — heredan automáticamente en toda la app
         ===================================================== -->

    <Style TargetType="TextBlock">
        <Setter Property="FontSize" Value="{StaticResource FontSizeBase}"/>
    </Style>

    <Style TargetType="TextBox">
        <Setter Property="FontSize" Value="{StaticResource FontSizeBase}"/>
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="FontSize" Value="{StaticResource FontSizeBase}"/>
    </Style>

    <Style TargetType="Button">
        <Setter Property="FontSize" Value="{StaticResource FontSizeBase}"/>
    </Style>

    <Style TargetType="CheckBox">
        <Setter Property="FontSize" Value="{StaticResource FontSizeBase}"/>
    </Style>

    <Style TargetType="DatePicker">
        <Setter Property="FontSize" Value="{StaticResource FontSizeBase}"/>
    </Style>

    <Style TargetType="DataGrid">
        <Setter Property="FontSize" Value="{StaticResource FontSizeBase}"/>
    </Style>

    <Style TargetType="DataGridColumnHeader">
        <Setter Property="FontSize" Value="{StaticResource FontSizeMedium}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

</ResourceDictionary>
