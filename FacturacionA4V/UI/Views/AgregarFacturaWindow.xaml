<Window x:Class="FacturacionA4V.UI.Views.AgregarFacturaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Agregar factura"
        Width="360"
        Height="260"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Esta factura se adjuntará a los registros seleccionados"
                   FontWeight="SemiBold"
                   Grid.Row="0"/>

        <StackPanel Grid.Row="1" Margin="0,12,0,0">
            <TextBlock Text="N° de factura"/>
            <TextBox Text="{Binding NroFactura, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,8,0,0">
            <TextBlock Text="Fecha de factura"/>
            <DatePicker SelectedDate="{Binding FechaFactura}"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,8,0,0">
            <TextBlock Text="Nota"/>
            <TextBox Text="{Binding Nota}" Height="60" TextWrapping="Wrap"/>
        </StackPanel>

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">

            <Button Content="Cancelar"
                    Width="90"
                    Margin="0,0,8,0"
                    IsCancel="True"/>

            <Button Content="Agregar"
                    Width="90"
                    IsDefault="True"
                    IsEnabled="{Binding IsValid}"
                    Click="OnAceptar"/>
        </StackPanel>
    </Grid>
</Window>